#include<stdio.h>
int main()
{
   int number;
   scanf("%d",&number);
      if(number%2==0)
      {
         printf("the given number is odd numbers");
         }
       else
       {
       printf("the given number is even number");
       }
       return 0;
       }
